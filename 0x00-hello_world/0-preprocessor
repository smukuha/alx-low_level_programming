#!/bin/bash

# Get the C file name from the $CFILE environment variable
c_file="$CFILE"

# Define the output file name
output_file="c"

# Run the C file through the preprocessor and save the result into the output file
cpp "$c_file" -o "$output_file"
